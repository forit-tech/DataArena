# DataArena

**Локальная рабочая среда для табличных данных: открыть, понять, почистить, собрать, выгрузить.**

Проект FORIT TECH.

---

> ## Текущее состояние: Этап 1 — скелет и контракты
>
> Работает: запуск backend и frontend, проверка связи между ними, единый контракт ошибок,
> structured logging, CI, тест на граф импортов между слоями.
>
> **Разделов работы с данными ещё нет.** Workspace, Dataset, SQL, Health, Builder и Recipes
> перечислены в навигации, но открывают экран, который прямо говорит, что раздел не реализован.
> Кнопок, за которыми ничего нет, в интерфейсе не будет.
>
> Контракт с ModelArena — [заморожен](docs/contracts/), версия `dataarena.package/1` 1.0.0.

---

## Зачем

Типичная задача: есть один или несколько файлов с данными. Нужно понять, что внутри,
проверить качество, исправить проблемы, выбрать нужное, объединить источники,
собрать итоговый датасет и сохранить в нужном формате.

Обычно это делается в ноутбуке: двадцать ячеек `pandas`, которые никто не воспроизведёт
через месяц. DataArena — попытка сделать ту же работу в инструменте, где каждое действие
остаётся шагом, который видно, можно отменить, сохранить рецептом и повторить.

**DataArena отвечает только за слой данных.** Модели обучает [ModelArena](https://github.com/forit-tech/ModelArena) —
отдельный сервис. DataArena полностью работает без него.

## Что планируется

| Раздел | Назначение | Этап |
|---|---|---|
| **Workspace** | несколько датасетов разных форматов одновременно | 2 |
| **Dataset** | таблица на миллион строк: сортировка, фильтры, статистика по колонке | 2 |
| **SQL** | запросы к открытым датасетам, история, сохранённые запросы | 3 |
| **Health** | профиль и находки, у каждой — конкретные строки, а не только счётчик | 4 |
| **Builder** | сборка нового датасета из нескольких источников через join | 6 |
| **Recipes** | рецепт сборки, применимый к новым версиям файлов | 9 |

Форматы к первому релизу: CSV, TSV, Parquet, JSON, JSONL/NDJSON, XLSX, Feather, Arrow IPC —
на чтение и на запись.

## Установка

Нужны Python 3.12+ и Node 22+.

```bash
python -m venv .venv
.venv/Scripts/pip install -e ".[dev]"     # Linux/macOS: .venv/bin/pip
cd frontend && npm install
```

## Запуск

Два процесса. Backend:

```bash
.venv/Scripts/python -m uvicorn backend.main:app --host 127.0.0.1 --port 8510
```

Frontend:

```bash
cd frontend && npm run dev
```

Интерфейс — `http://127.0.0.1:5174`, документация API — `http://127.0.0.1:8510/docs`.

Ни одна переменная окружения не обязательна. Полный список с пояснениями — в [`.env.example`](.env.example).

## Разработка

```bash
.venv/Scripts/python -m pytest        # тесты backend
.venv/Scripts/ruff check backend tests
.venv/Scripts/mypy
cd frontend && npm run lint && npx tsc --noEmit && npm run test && npm run build
```

Контракт Dataset Package проверяется отдельно и в CI прогоняется на двух версиях Polars:

```bash
python docs/contracts/reference/run_matrix.py docs/contracts
python docs/contracts/reference/check_reproducibility.py
```

## Архитектура

```
backend/
  api/        HTTP: роутеры, схемы запросов и ответов, обработчики ошибок
  services/   сценарии и оркестрация
  domain/     предметные модели и чистые вычисления — не знает про HTTP, файлы и БД
  adapters/   форматы, хранилище, движок SQL
  core/       конфиг, ошибки, логирование
```

Направление зависимостей `api → services → domain` проверяется
[тестом](tests/unit/test_import_graph.py), а не договорённостью: один импорт «по-быстрому»
не виден в ревью, а через месяц доменную логику уже нельзя протестировать без FastAPI.

Обоснование каждого решения и каждой зависимости — в [ARCHITECTURE.md](docs/ARCHITECTURE.md).

## Документы

| Документ | О чём |
|---|---|
| [AUDIT.md](AUDIT.md) | Аудит предшественника, AutoDataAnalysis. Всё проверено запуском: 148 + 32 теста, замеры на 1 000 000 строк, попытки обхода SQL-песочницы |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | Целевая архитектура и обоснование решений |
| [docs/MIGRATION_MAP.md](docs/MIGRATION_MAP.md) | Что переносится из AutoDataAnalysis, что переписывается, что уходит в ModelArena |
| [docs/IMPLEMENTATION_PLAN.md](docs/IMPLEMENTATION_PLAN.md) | Этапы 1–11 и критерии готовности |
| [docs/RISKS.md](docs/RISKS.md) | Технические риски |
| [docs/contracts/](docs/contracts/) | Dataset Package `dataarena.package/1` — спецификация, JSON Schema, эталонные реализации, golden package |

## Стек

Python 3.12 · FastAPI · Pydantic · Polars · DuckDB · SQLite
React 19 · TypeScript · Vite

Зависимостей frontend ровно три: `react`, `react-dom`, `lucide-react`. UI-фреймворка нет —
свой CSS даёт полный контроль над плотностью интерфейса.

## Ограничения

- **Аутентификации нет.** Это локальный однопользовательский инструмент; backend слушает
  `127.0.0.1`. При выносе наружу аутентификация стала бы обязательной.
- Скриншотов в README пока нет: показывать нечего, кроме оболочки. Они появятся вместе
  с работающими разделами и будут настоящими.

## Происхождение

DataArena вырастает из [AutoDataAnalysis](https://github.com/forit-tech/AutoDataAnalysis) —
работающего инструмента, который совмещал слой данных и ML. Аудит показал, что слой данных
переиспользуем примерно на 60%, но его архитектурное основание — stateless-backend, где каждая
операция заново загружает файл целиком — блокирует большинство требований к DataArena.

Поэтому проект разделён на два сервиса, а не переписан на месте. Подробности —
в [AUDIT.md](AUDIT.md), находка A-1.

## Лицензия

Будет определена до первого публичного релиза.
